import data from "./data_module.js";
console.log(data);
//Sử dụng arrow thực hiện các tác vụ:
/**
 * 1. In ra các danh sách các đối tượng: id, name, price, quantity
 * 2. In ra danh sách các đối tượng có id được truyền từ người dùng
 * 3. In ra danh sách các đối tượng có price > 20, < 100
 * 4. In ra danh sách các đối tượng được sắp xếp theo 2 tiêu chí: name và price
 *    Nếu trùng name thì sắp xếp theo price giảm dần
 * 5. Thêm mới 1 đối tượng được cung cấp từ người dùng -> Gọi lại 1. để kiểm tra
 * 6. Cập nhật thông tin của 1 đối tượng (với id chỉ định) theo dữ liệu của người dùng -> Gọi lại 2. để kiểm tra
 * 7. Xóa 1 đối tượng theo id -> Gọi lại 1. để kiểm tra
 */
